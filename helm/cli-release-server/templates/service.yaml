apiVersion: v1
kind: Service
metadata:
  name: {{ include "cli-release-server.fullname" . }}
  labels:
    {{- include "cli-release-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "cli-release-server.selectorLabels" . | nindent 4 }}
